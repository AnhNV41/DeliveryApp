<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DeliveryApp.Helper;assembly=DeliveryApp"
             x:Class="DeliveryApp.Views.CategoryDetailPage" BackgroundColor="#F0F3F4"
             x:Name="ParentView">
    <StackLayout>
        <Frame Background="White" Padding="0" HeightRequest="280">
            <StackLayout Spacing="20" Margin="10,0,10,10">
                <StackLayout Orientation="Horizontal">
                    <Frame Padding="5" Grid.Column="0" VerticalOptions="Center" HeightRequest="30" WidthRequest="30" CornerRadius="10" BackgroundColor="#F0F3F4">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding BackCommand}"/>
                        </Frame.GestureRecognizers>
                        <Image  Source="{local:ImageResource DeliveryApp.Resource.arrow-left.png}" Margin="5,5,5,5"/>
                    </Frame>
                    <Frame WidthRequest="300"/>
                    <Frame  Padding="5"  VerticalOptions="Center" HeightRequest="30" WidthRequest="30" CornerRadius="10" BackgroundColor="#F0F3F4 ">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding SearchCommand}"/>
                        </Frame.GestureRecognizers>
                        <Image Source="{local:ImageResource DeliveryApp.Resource.blacksearch.png}"/>
                    </Frame>
                    <Frame Padding="5" VerticalOptions="Center" HeightRequest="30" WidthRequest="30" CornerRadius="10" BackgroundColor="#EAECEE">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding FilterCommand}"/>
                        </Frame.GestureRecognizers>
                        <Image x:Name="Fillter" Source="{local:ImageResource DeliveryApp.Resource.filter.png}"/>
                    </Frame>
                </StackLayout>
                <Label Text="{Binding PageTitle}" FontAttributes="Bold" FontSize="30" VerticalOptions="Center" HorizontalOptions="Start"/>
                <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
                    <Grid  HeightRequest="50" Padding="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Frame Grid.Column="0" BackgroundColor="#7DB408" CornerRadius="17" Padding="0" VerticalOptions="Center" HorizontalOptions="Center" HeightRequest="60" WidthRequest="60">
                            <Label Text=" All" FontAttributes="Bold"  TextColor="White" HorizontalOptions="Center" VerticalOptions="Center"/>
                        </Frame>
                        <CollectionView Grid.Column="1"
                         ItemsSource="{Binding CategoryItems}"
                         VerticalScrollBarVisibility="Never" 
                        HorizontalScrollBarVisibility="Never"
                        SelectionMode="Single"
                         SelectedItem="{Binding FillterSelectedItem}"
                        VerticalOptions="CenterAndExpand" HorizontalOptions="Center" >
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Grid  Margin="10,0,0,0" Padding="0">
                                        <Frame BackgroundColor="#F0F3F4" CornerRadius="17" Padding="0" VerticalOptions="Center" HorizontalOptions="CenterAndExpand" HeightRequest="60" WidthRequest="150">
                                            <Label Text="{Binding Title}" FontSize="15" FontAttributes="Bold"  TextColor="Black" HorizontalOptions="CenterAndExpand" VerticalOptions="Center"/>
                                        </Frame>
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                            <CollectionView.ItemsLayout>
                                <GridItemsLayout Orientation="Horizontal"
                                                 Span="1" />
                            </CollectionView.ItemsLayout>
                        </CollectionView>
                    </Grid>
                </ScrollView>
            </StackLayout>
        </Frame>
        <CollectionView
                    ItemsSource="{Binding CategoryItems}"
                    VerticalScrollBarVisibility="Never" 
                    SelectedItem="{Binding SelectedItem}"  
                    SelectionMode="Single"
                    Margin="10,10,10,0" 
                    HorizontalOptions="Center" 
                    VerticalOptions="Center">
            
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" HeightRequest="300" >
                        <Frame BackgroundColor="White" CornerRadius="20" HasShadow="True">
                            <StackLayout Orientation="Vertical" Spacing="0" Margin="5,5,5,5">
                                <ImageButton Source="{local:ImageResource DeliveryApp.Resource.heart.png}" HeightRequest="20"  WidthRequest="20" HorizontalOptions="End" BackgroundColor="White" Opacity="0.25" Command="{Binding LikedCommand}">
                                    <ImageButton.Triggers>
                                        <DataTrigger TargetType="ImageButton"
                                                     Binding="{Binding Path=Like}"
                                                     Value="True">
                                            <Setter Property="Opacity" Value="1" />
                                        </DataTrigger>
                                    </ImageButton.Triggers>
                                </ImageButton>
                                <Image  Source="{Binding ImageSource}" HorizontalOptions="Center" VerticalOptions="Start" Margin="0,0,5,0"/>

                                <!--<Frame Margin="10,0,10,0" Padding="0" IsClippedToBounds="True" HeightRequest="300" BackgroundColor="White" >
                                    <Image  Source="{Binding ImageSource}" HorizontalOptions="Center" VerticalOptions="Center" />
                                </Frame>-->
                                <Label Text="{Binding Title}" FontSize="15" FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Start"/>
                                <Label Text="{Binding DisplayUnitPrice}" FontAttributes="Bold"/>
                                <StackLayout Orientation="Horizontal"  Margin="0,10,0,0" Spacing="5">
                                    <Frame BackgroundColor="#7DB408" WidthRequest="20" CornerRadius="20" HorizontalOptions="Center" VerticalOptions="Center" Padding="5">
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding SubtractCommand, Mode=TwoWay}"/>
                                        </Frame.GestureRecognizers>
                                        <Image Source="{local:ImageResource DeliveryApp.Resource.subtract.png}" WidthRequest="20"/>
                                    </Frame>
                                    <Label Text="{Binding Quantity,Mode=TwoWay}" WidthRequest="25" HorizontalTextAlignment="Center" VerticalOptions="Center" HorizontalOptions="CenterAndExpand" />
                                    <Frame BackgroundColor="#7DB408" WidthRequest="20" CornerRadius="20" HorizontalOptions="Center" VerticalOptions="Center" Padding="5">
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding PlusCommand, Mode=TwoWay}"/>
                                        </Frame.GestureRecognizers>
                                        <Image Source="{local:ImageResource DeliveryApp.Resource.plus.png}" WidthRequest="20"/>
                                    </Frame>
                                </StackLayout>
                            </StackLayout>
                        </Frame>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical"
                        Span="2" />
            </CollectionView.ItemsLayout>
        </CollectionView>
        <Frame HeightRequest="100" BackgroundColor="White">
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0"  Source="{local:ImageResource DeliveryApp.Resource.home.png}"    HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="30" Opacity="0.25"/>
                <Image Grid.Column="1" Source="{local:ImageResource DeliveryApp.Resource.grid.png}"    HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="30" Opacity="0.25"/>
                <Frame Grid.Column="2" Padding="5" CornerRadius="10" BackgroundColor="#7DB408">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding CartTapCommand}"/>
                    </Frame.GestureRecognizers>
                    <Image  Source="{local:ImageResource DeliveryApp.Resource.shopping-cart.png}"   HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="30" Opacity="0.25"/>
                </Frame>
                <Image Grid.Column="3" Source="{local:ImageResource DeliveryApp.Resource.love.png}"    HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="30" Opacity="0.25"/>
                <Image Grid.Column="4" Source="{local:ImageResource DeliveryApp.Resource.user.png}"    HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="30" Opacity="0.25"/>
            </Grid>
        </Frame>

        <!--<Frame HeightRequest="50" BackgroundColor="White">
            <StackLayout Orientation="Horizontal" WidthRequest="300" Spacing="20" >
                <Frame Padding="10">
                    <Image Source="{local:ImageResource DeliveryApp.Resource.home.png}" HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="50" Opacity="0.25"/>
                </Frame>
                <Frame Padding="10">
                    <Image Source="{local:ImageResource DeliveryApp.Resource.grid.png}"  HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="50" Opacity="0.25"/>
                </Frame>
                <Frame Padding="10" CornerRadius="10" BackgroundColor="#7DB408">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding CartTapCommand}"/>
                    </Frame.GestureRecognizers>
                    <Image  Source="{local:ImageResource DeliveryApp.Resource.shopping-cart.png}" HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="50" />
                </Frame>
                <Frame Padding="10">
                    <Image Source="{local:ImageResource DeliveryApp.Resource.love.png}" HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="50" Opacity="0.25"/>
                </Frame>
                <Frame Padding="10">
                    <Image Source="{local:ImageResource DeliveryApp.Resource.user.png}" HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="50" Opacity="0.25"/>
                </Frame>
            </StackLayout>
        </Frame>-->
    </StackLayout>
</ContentPage>